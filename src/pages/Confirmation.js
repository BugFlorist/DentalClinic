import React from "react";
import { Breadcrumb, Carousel, Card, Button } from "react-bootstrap";

function Home() {
    return (
        <div>
            <h1> Confirmed</h1>
        </div>
    );
}

export default Home;